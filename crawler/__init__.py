"""Crawler package.

Modules:
    frontier   -- URL queue + BFS depth tracking (P2)
    downloader -- HTTP fetching (P3)
    parser     -- HTML link extraction + URL normalization (P4)
    filters    -- duplicate / domain / extension filtering (P5)
    storage    -- save HTML pages and metadata (P5)
    seeds      -- load seed URLs from file (P1)
"""
